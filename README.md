# Aproximados

Drinking game web para previas. Monorepo listo para crecer.

```
Frontend/    Angular 20 + Tailwind (PWA)
# Backend/   (cuando toque: API de salas)
```

## Arrancar

```bash
npm start
```

Sirve el frontend en `http://localhost:4200`. También puedes entrar en `Frontend/` y usar `npm start` igual.
