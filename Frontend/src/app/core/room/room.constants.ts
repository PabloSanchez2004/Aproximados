/** Longitud fija del código de sala. 4 letras: lo justo para gritarlo por el salón. */
export const ROOM_CODE_LENGTH = 4;

/** Sin vocales para evitar palabrotas y sin I/O para no confundirlas con 1/0. */
export const ROOM_CODE_ALPHABET = 'BCDFGHJKLMNPQRSTVWXYZ';

/** Huecos del lobby. 8 da para una previa; el grid los reparte 3/4 columnas. */
export const LOBBY_SLOT_COUNT = 8;
